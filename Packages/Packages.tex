% -----------------------------------------------------------------------------
                                % Standard Packages
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
    \setlength\parindent{0pt} % set distance between paragraphs to 0
% -----------------------------------------------------------------------------
                                % Enhances mathematical formulas
\usepackage{amsmath}
% -----------------------------------------------------------------------------
                                % Includes math symbols
\usepackage{amsfonts}
% -----------------------------------------------------------------------------
                                % Include integral symbols
\usepackage{esint}
% -----------------------------------------------------------------------------
                                % Include latex symbols
\usepackage{latexsym}
% -----------------------------------------------------------------------------
                                % Include referencing
\usepackage{hyperref}
    % URLs: \href{insert url here}{text}
% -----------------------------------------------------------------------------
                                % Indent first paragraph
% Optional: \usepackage{indentfirst}
% -----------------------------------------------------------------------------
                                % Creates header
\usepackage{lastpage}
\usepackage{fancyhdr}
    \pagestyle{fancy}
    \fancyhf{}
    \lhead{\small \leftmark}       % adds chapter name to left header
    \rhead{\small \rightmark}      % adds section name to right header
    \cfoot{Page $\thepage$ out of $\pageref*{LastPage}$}    % adds page numbers

    % create custom header for first page
    \fancypagestyle{customfirst}{
        \renewcommand{\headrulewidth}{0pt}
        \cfoot{Page $\thepage$ out of $\pageref*{LastPage}$}
    }
% -----------------------------------------------------------------------------
                                % Adjust line spacing
\usepackage{setspace}
    \singlespacing
% -----------------------------------------------------------------------------
                                % Includes specialized lists
\usepackage{paralist}
    % Format
        % \begin{compactenum}
        %   \item Item 1
        %   \begin{compactitem}
        %       \item Subitem
        %   \end{compactitem}
        % \end{compactenum}
        
\usepackage{enumitem}
    % Format
		% \setlist{nolistsep}
        % \setitemize[1]{label = insert label here}
        % \setenumerate[1]{label = insert label here}
        %
        % \begin{enumerate}
		%	\item Item
        %   \begin{itemize}
        %       \item Subitem
        %   \end{itemize}
        % \end{enumerate}

    \setlist{nolistsep}         % spacing between lists and other stuff set to 0
% -----------------------------------------------------------------------------
                                % Adjust tables
\usepackage{array}
    \setlength{\extrarowheight}{2pt}        % adds more height to table rows
            % Optional: \usepackage{booktabs}
    % Format
        % \begin{table}[placement]  i.e)   h = here, t = top, b = bot
        %   \centering
        %   \begin{tabular}{|c}     i.e)   | = vertical line, c = column
        %       \stuff
        %   \end{tabular}
        % \caption{Name of table}
        % \label{Table label}    
        % \end{table}
% -----------------------------------------------------------------------------
                                % Include pictures
\usepackage{graphicx}
    % Format
        % \begin{figure}[h]
        %   \centering
        %   \includegraphics[scale=0.25]{Picture}
        %   \caption{Name of picture}
        %   \label{Picture label}
        % \end{figure}
% -----------------------------------------------------------------------------
                                % Include colors (also to tables)
\usepackage{xcolor}
    % Format
    %   \color{insert color here}
    %   \definecolor{name}{RGB}{#, #, #}
\usepackage{colortbl}
    \definecolor{purple}{RGB}{210, 0, 235}
    \definecolor{lblue}{RGB}{10, 180, 250}
% -----------------------------------------------------------------------------  
                                % Adjust paper size
\usepackage[a4paper, inner=3cm, outer=3cm, top=2.5cm, bottom=2.5cm]{geometry}
    \setlength\headheight{15pt}         % set heigh of header to 15pt
    \renewcommand{\headwidth}{15cm}     % set width of header to 15cm
% -----------------------------------------------------------------------------
								% Include Textboxes
\usepackage{adjustbox}
    % \begin{adjustbox}{minipage=14cm, right}
    % \end{adjustbox}
% -----------------------------------------------------------------------------
								% Include crossed out statements
\usepackage[thicklines]{cancel}
\renewcommand\CancelColor{\color{red}}
    % \cancel{text} = strikethrough from bot to top
    % \bcancel{text} = strikethrough from top to bot
    % \xcancel{text} = strikethrough form X
    % \cancelto{value}{text} = strikethrough with superscript value